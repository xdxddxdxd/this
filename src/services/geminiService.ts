export const geminiService={async extractTextFromImage(base64Image:string):Promise<string>{if(!base64Image)throw new Error('Görsel bulunamadı.');const m=base64Image.match(/^data:([^;]+);base64,/i);const r=await fetch('/.netlify/functions/ai-ocr',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:base64Image,mimeType:m?.[1]||'image/jpeg'})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`OCR HTTP ${r.status}`);return String(d.text||'').trim();}};
